# 変更履歴

版は日付で管理する。番号に意味はない。

English: [CHANGELOG.md](CHANGELOG.md)

## 2026-09-13

- `--thumbnails DIR` を追加。メタデータに見本画像（`modelspec.thumbnail`）を
  持つファイルから、それをファイルとして書き出す。走査した構造を再現し、
  対象を複数指定したときはそれぞれにサブフォルダを作る。
  指定したフォルダの外には書かない。
- 端末での表示を、文字数から中身の説明に変えた。
  `見本画像: JPEG 画像、9.8 KB` と出る。`--meta` を付けたときは
  base64 の全文ではなく data URI の冒頭だけを出す。
- 宣言された種別は信用しない。何であるかは先頭のバイトで決める。
  `image/jpeg` と書かれた PNG は `.png` として書き出す。画像でないものは
  書き出さず、宣言されていた種別つきで報告する（`画像ではない: video/mp4`）。
- HTML レポートも同じ判定を通すようにした。描けない値は壊れた `<img>` ではなく、
  理由の文として出る。

追加にあたって実測したこと。このプロジェクトが検証に使っている公開モデル 47 件のうち、
見本画像を持つのは 7 件で、いずれも Stability AI か Black Forest Labs が配布した
チェックポイントかバックボーンだった。動画モデルは 1 件も持っていない。

## 2026-09-07

- README の先頭にバナーを置いた。

## 2026-09-05

- 走査を始めるだけの小さな窓 `stgui.py`。**「レポートを開く」** ボタンで、
  前回の走査が残したレポートも開き直せる。
- 自己完結の HTML レポート。並べ替えと検索ができる一覧で、行を開くと詳細が出る。
  フォルダのサマリと、判別できなかったファイルの詳細も同じファイルに入る。
- メタデータのキーに、意味と「そのまま読むと誤解する点」を添えた。
  ファイルではなくベースモデルを指しているタイトル、別物のライセンスなど。
- フォルダ走査の末尾にサマリを出し、判別できなかったファイルを
  ルールに必要なトップレベルのキー名つきで並べるようにした。
- マージの記録を、どこに書かれていても読むようにした。
- レポートに、対象フォルダから作った既定の名前を付けるようにした。

## 2026-09-04

- 最初の公開。SD1.x、SDXL、FLUX、SD3.5、Qwen-Image、Wan、HunyuanVideo、
  HunyuanDiT、Stable Cascade、LTX-Video、CogVideoX、Mochi 1、SANA、PixArt、
  AuraFlow、Lumina 2.0、Chroma、HiDream-I1、Z-Image の判定ルール。
  いずれも記憶ではなく実ファイルのヘッダで確認したもの。
- `tools/verify_rules.py`。公開モデルのヘッダだけを HTTP Range で取得して
  ルールと照合する。
- `tools/self_check.py`。プロジェクト自身の整合を検査する。
  ドキュメントに書かれた件数、何にもマッチしないルール、
  片方の言語にしかないメッセージを見る。
